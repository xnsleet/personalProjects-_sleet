server:
  port: 8088
spring:
  application:
    name: personalProject_sleet #指定服务名
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/personalProject_sleet?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8
    username: root
    password: root
  jpa:
      database: MySQL
      show-sql: true
      generate-ddl: true

